export type ConsoleIntegration = { id: "telegram" | "openai" | "google_calendar" | "trello"; label: string; readiness: "ready" | "attention" | "not_configured"; detail: string; lastActivityAt?: string };
export type ConsoleLead = { businessReference: string; lifecycle: "new_lead" | "qualified" | "booked" | "done" | "lost"; cleaningType?: "standard" | "deep"; areaM2?: number; preferredDate?: string; humanNeeded: boolean; humanNeededReason?: string; quotedPriceRsd?: number; assignedTeam?: "team_a" | "team_b"; bookedStart?: string; trelloCardUrl?: string };
export type ConsoleActivity = { eventType: string; occurredAt: string };
export type ConsoleReadModel = { generatedAt: string; snapshotKind: "application_snapshot"; providerState: "not_live_checked"; currentStatus: "terminal" | "human_needed" | "recovery" | "pending" | "stalled" | "waiting_customer" | "idle"; currentStatusDetail: string; integrations: ConsoleIntegration[]; latestLead?: ConsoleLead; latestLeadActivity: ConsoleActivity[]; trelloBoard: { kind: "projection" | "not_configured" | "unavailable"; boardUrl?: string; cards: Array<{ title: string; businessReference: string; lifecycle: ConsoleLead["lifecycle"]; humanNeeded: boolean; updatedAt: string; cardUrl?: string }> } };
